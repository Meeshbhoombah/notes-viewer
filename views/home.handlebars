<nav class="navbar navbar-toggleable-md fixed-bottom navbar-dark bg-faded">
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <a class="navbar-brand" href="/">Strains</a>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="/review/new">NEW REVIEW<span class="sr-only">(current)</span></a>
      </li>
    </ul>
  </div>
</nav>
{{#each review}}
<div class='container-fluid'>
    <div class='row'>
        <div class='col-md-push-4 col-md-4'>
            <h2 style='color: #ffffff;'>{{title}}</h2>
            <p style='color: #ffffff;'>{{text}}</p>
            <form class='editReview' method='POST' action='/review/edit'>
                <input type="hidden" name="id" value='{{_id}}'>
                <button class='btn btn-danger' type='submit'>EDIT</button>
            </form>
            <form class='editReview' method='POST' action='/review/delete'>
                <input type="hidden" name="id" value='{{_id}}'>
                <button class='btn btn-danger' type='submit'>DELETE</button>
            </form> 
        </div>
    </div>
</div>
{{/each}}
